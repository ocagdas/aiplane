interface:
  display_name: "aiplane"
  short_description: "Inspect aiplane environment-configuration and exports"
  default_prompt: "Use the $aiplane skill to diagnose an environment and preview configuration exports; follow contributor guidance when modifying its source."
